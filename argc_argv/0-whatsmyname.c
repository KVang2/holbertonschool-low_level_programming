#include <stdio.h>
/**
 * main - printing name, followed by new line
 * @argc: number
 * @argv: array
 * Return: 0
*/
int main(int agrc __attribute__((unused)), char * argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
