#include "main.h"
/**
 * _strlen - returns the length of a string
 *
 * @s: string
 *
 * Return: count
*/
int _strlen(char *s)
{
	int l;

	for (l = 0; s[l] != '\0'; ++l);
	{
		_putchar(*s);
	}
	_putchar('\n');
	}
return count;
}
