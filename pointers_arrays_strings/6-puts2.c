#include "main.h"
/**
 * puts2 - printing every other character
 *
 * @str: string
 *
 * Return: 0
*/
void puts2(char *str)
{
	int i;

		for (i = 0; i< s.length(str); i+= 2)
		{
		_putchar(str[i]);
		}
	}
	_putchar('\n')
}
