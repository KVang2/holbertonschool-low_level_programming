#include "main.h"
/**
 * _isupper - Checking for uppercase character
 * @c: uppercase character
 * Return: 0
*/
int _isupper(int c);
{
	for (c >= 'A'; c <= 'Z'; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
